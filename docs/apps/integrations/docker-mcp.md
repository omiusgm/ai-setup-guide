---
title: "Docker MCP Toolkit — каталог MCP"
category: integrations
tags:
  - mcp
  - технарям
updated: 2026-04-16
---

# 🐳 Docker MCP Toolkit

> 300+ готовых MCP-плагинов в каталоге Docker Desktop. Установка в один клик — никаких npm, pip, терминала.

## Что делает

Docker Desktop добавил раздел «MCP Toolkit». Открываешь — видишь каталог на 300+ MCP-серверов: Gmail, Slack, GitHub, базы данных, AWS, платежки. Каждый ставится в один клик. Не надо возиться с конфигами.

Сами MCP запускаются в Docker-контейнерах — изолированно, не мусорят в системе. Удалил — ничего не осталось.

## Кому подходит

- ✅ Docker Desktop уже стоит
- ✅ Хочешь пробовать MCP без боли с установкой
- ✅ Работаешь с корпоративными сервисами (AWS, Azure, GCP)
- ❌ НЕ подходит если не хочешь ставить Docker
- ❌ На слабом компе — Docker ест ресурсы

## Как попробовать без терминала

Docker MCP Toolkit — как раз тот редкий случай, когда всё ставится мышкой. Терминал не нужен:

1. Скачай **[Docker Desktop](https://www.docker.com/products/docker-desktop)** — обычный установщик (Mac, Windows, Linux)
2. Запусти — слева в панели появится «MCP Toolkit»
3. Открой вкладку → каталог плагинов (GitHub, Gmail, Postgres, AWS, Jira...)
4. Жмёшь «Install» на нужном — плагин поднимается сам в контейнере
5. Теперь он доступен в Claude Code и других MCP-клиентах

Это самый простой способ пощупать MCP без возни с конфигами и установками.

??? note "Для продвинутых (CLI)"

    Docker Desktop сам по себе не требует терминала. Но если хочешь управлять MCP-контейнерами из командной строки — у Docker есть свой CLI:

    ```bash
    docker mcp list
    docker mcp install <plugin-name>
    ```

    Удобно если настраиваешь окружение скриптом или в CI.

## Примеры

- **GitHub MCP** — ИИ читает/пишет issue, PR, код
- **Postgres MCP** — ИИ выполняет SQL-запросы
- **AWS MCP** — ИИ управляет облаком
- **Gmail MCP** — читает/пишет письма

## Нюансы

- Docker Desktop ест 2-4 ГБ RAM — на слабых компах тяжело
- Некоторые MCP требуют API ключи — подставь в настройках
- Не все MCP одинаково качественные — смотри рейтинг в каталоге

## Ссылки

- [Docker Desktop](https://www.docker.com/products/docker-desktop)
- [MCP Toolkit документация](https://docs.docker.com/desktop/mcp/)
- Альтернативы: [Composio](composio.md), ручная установка MCP

---

*Подходит для: технарям · корпоративному стеку · простоте установки*
